export interface CipDec {
   cipher (str: string) : string;
   decipher (str: string) : string;
  // cipherDecipher(str: string, mapperFun: (symb: string) => string): string;
   //mapperCipher(symb: string) : string;

}